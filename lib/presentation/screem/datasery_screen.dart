/* import 'package:flutter/material.dart';

class DataseryScreen extends StatefulWidget {
  const DataseryScreen({Key key}) : super(key: key);

  @override
  _DataseryScreenState createState() => _DataseryScreenState();
}

class _DataseryScreenState extends State<DataseryScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Hello2"),
      ),

      // This trailing comma makes auto-formatting nicer for build methods.
    );
  }
}
 */